# GitHub手动推送步骤指南

## 🚀 快速执行方法

### 方法1：使用一键推送脚本（推荐）
1. 双击运行 `一键推送GitHub.bat`
2. 按提示输入GitHub认证信息
3. 等待推送完成

### 方法2：使用快速推送脚本
1. 双击运行 `快速推送.bat`
2. 按提示输入GitHub认证信息

### 方法3：手动命令（如果脚本不工作）
1. 在项目目录中打开命令行
2. 逐行执行以下命令：

```cmd
git init
git remote add origin https://github.com/mochensissy/251122-2.git
git add .
git commit -m "初始提交"
git push -u origin main
```

## 📝 详细手动步骤

### 第一步：打开命令行
- **方法1**: 在项目文件夹中，按住 `Shift` + 右键，选择"在此处打开命令窗口"
- **方法2**: 打开 `cmd` 或 `PowerShell`，使用 `cd` 命令导航到项目目录
- **方法3**: 在VS Code中按 `Ctrl + `` 打开终端

### 第二步：检查Git安装
```cmd
git --version
```
如果没有安装Git，请下载并安装：https://git-scm.com/download/win

### 第三步：初始化仓库
```cmd
git init
```

### 第四步：配置用户信息（如果尚未配置）
```cmd
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

### 第五步：添加远程仓库
```cmd
git remote add origin https://github.com/mochensissy/251122-2.git
```

### 第六步：添加文件
```cmd
git add .
```
或者只添加重要文件：
```cmd
git add src/
git add package.json package-lock.json
git add *.json *.js *.ts *.tsx *.html *.md
git add prd/ supabase/ .gitignore
```

### 第七步：创建提交
```cmd
git commit -m "初始提交：职业发展AI智能体系统 v2.3"
```

### 第八步：推送到GitHub
```cmd
git push -u origin main
```

## 🔐 GitHub认证

### 使用用户名和密码（不推荐）
- 用户名：你的GitHub用户名
- 密码：你的GitHub密码

### 使用Personal Access Token（推荐）
1. 登录GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 生成新token，勾选 `repo` 权限
3. 在密码提示时，使用token替代密码

## ❗ 常见问题解决

### 错误：Authentication failed
**解决方案**：使用Personal Access Token替代密码

### 错误：Repository not found
**解决方案**：确认仓库地址正确且你有访问权限

### 错误：src does not match any file
**解决方案**：确认在正确的项目目录中执行命令

### 错误：LF will be replaced by CRLF
**警告信息**，不影响推送，可以忽略

## ✅ 验证推送成功

推送成功后：
1. 访问 https://github.com/mochensissy/251122-2
2. 应该能看到完整的项目文件
3. README.md会显示为项目首页

## 📞 需要帮助？

如果仍然遇到问题：
1. 检查GitHub仓库是否存在且为空
2. 确认网络连接正常
3. 验证GitHub账户权限
4. 尝试使用GitHub Desktop客户端
