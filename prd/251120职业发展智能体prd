 **产品需求文档："职业发展打样"AI政策解答与学习闭环智能体 \- V2.2** 

## **0. 当前落地对照与调整建议（Codex补充）**

### **0.1 项目整体现状概览**

- ✅ React + Supabase 单页应用已搭建完成，顶部角色切换器可以演示员工 / 专家 / 领导三端视角。
- ✅ 核心对话流集成企业级 RAG：`ragEngine` 负责文档分块、向量生成与混合检索，`searchKnowledge` 实现向量 + 关键词回退、免责声明拼装以及 Query Cache。
- ✅ HITL 闭环已具备雏形：员工端“上报问题”会写入 `escalation_tasks` 并触发邮件；专家填写答案后可提交领导审批，审批通过即写回知识库并清理缓存。
- ✅ 框架还扩展了 PRD 之外的能力：查询缓存 + 性能监控面板、用户反馈（点赞/点踩 + 评论）、批量文档处理器、DeepSeek Prompt 接口、多格式文件解析等。
- ⚠️ 仍有部分 PRD Must-have 功能未完全对齐，尤其是画像持久化、标签管理、闭环通知链接、共情提示词等，需集中补齐。

### **0.2 与 PRD 逐条对照**

| 模块 | PRD 要求 | 现状 | 差距/优化点 |
| --- | --- | --- | --- |
| 4.1.1 用户画像 | 首次/重置时收集序列、职级、角色并跨会话持久化 | UI 已存在三道必答题并写入 `user_profiles` | 启动时未读取持久画像，`anonymous_user` 每次重登需重新填写；无“重置画像”入口；专家端联系文案仍写死，未用 `system_settings`。 |
| 4.1.2 角色化 RAG | 用画像过滤RAG结果、建议类自动免责声明 | `searchKnowledge` 会带上 Metadata 过滤、无结果时回退传统检索，回答末尾根据关键词添加免责声明 | 建议类判定逻辑较弱（通过“建议”字串或 `doc_type === policy`）；缺少“推荐/建议”场景判别及配置化免责声明。 |
| 4.1.3 标签管理 | 上传文档时可打多标签，字段与画像一致且可修改/替换 | `FileUploader` 上传默认写“通用”标签，`DocumentList` 仅展示、删除、索引 | 缺少标签编辑界面与重新索引按钮；无版本替换；无 extra tags/动态字段。 |
| 4.2.1 员工闭环 | “联系专家/上报问题”两个明确选项；不满意可触发 | RAG 失败时弹出两按钮；另外的 RAGChat 有点赞/点踩功能 | 员工端没有“不满意”按钮，全依赖自动判定；上报提示 SLA 固定文案，未引用后台配置。 |
| 4.2.2 专家处理 | 邮件通知（可配置邮箱），带问题、画像、上下文和唯一链接；专家可查看完整对话、富文本答题 | 任务列表分“新咨询/被驳回”；可撰写并提交答案；`notificationService.notifyExpert` 调用 Edge Function | 邮件模板 `href="#"`，无实际链接/Token；任务卡未展示 `history`；联系专家话术不可配置；系统未强制单一专家（默认1人 ok）。 |
| 4.2.3 领导审批 | 邮件通知 + 审批界面，批准写回知识库并通知员工，驳回需填写理由并退回专家 | UI 支持批准/驳回；批准后写入 `knowledge_docs` 并清缓存 | 驳回时未强制填写 `rejectReason`；审批成功后未调用 `notifyEmployee`；邮件模板同样缺真实链接。 |
| 4.3.1 共情式回复 | Prompt 定义事实 / 共情双模式，关键词触发共情，输出结构=共情+客观+边界 | `detectEmotion` + `getEmpathyPrefix` 仅在员工端 RAG 回答前加一段共情文案 | DeepSeek prompt 未包含共情指南；共情模式只负责前缀，没执行三段式结构；情绪关键词列表也未统一写入 Prompt 或配置。 |
| 5.2 / 5.3 配置 | 专家/领导邮箱、联系文案、情绪关键词等应后台可配置；非目标包括知识图谱等 | `system_settings` 表已创建，设置页面可写邮箱和 SLA | 邮件地址外的配置（联系专家话术、情绪词、免责声明模板）仍写死在代码里；系统未读取持久设置。 |

### **0.3 现有新增能力（PRD 未提但值得保留）**

1. **查询缓存 + 性能监控**： `queryCache` 提供 LRU/TTL + 统计，`PerformanceMonitor` 面板显示命中率、响应时间、用户满意度、检索日志。
2. **用户反馈闭环**：RAGChat 端支持点赞/点踩和文字评论，数据写入 `user_feedback`、`feedback_stats`，同时配合 Performance Monitor 展示满意度。
3. **批量索引与任务队列**：`batchProcessor` + `RAGManagement` 支撑文档批量分块、并发索引、历史记录、失败重试，方便后续导入更多文档。
4. **DeepSeek Prompt & 文件解析**：封装 `deepseek-client`、多格式 `file-parser`，可为未来扩展（多模型、批量上传）打基础。

### **0.4 下一步落地计划**

**Step 1 — 画像与配置持久化（本轮启动）**
1. 登录后读取 `user_profiles` 中 `anonymous_user` 的最后一次画像，如存在则直接载入；保留“重置画像”入口以满足重填需求。
2. 将“联系专家”话术、SLA 提示改为读取 `system_settings`，并在设置面板中补充这两个字段，满足 PRD 5.2。
3. 对 `notificationService.notifyEmployee` 补齐调用链：审批通过后根据 `asker_profile` 找到对应邮箱（暂可放置在系统配置或画像扩展字段）。
4. 更新 PRD 5.2，明确画像持久化、联系话术配置和员工邮箱的落地路径。

**Step 2 — 标签管理与 RAG 策略优化**
1. 在上传/编辑文档时，允许设置 sequence/level/role 三个基础标签和 extra tags；修改现有文档时触发 `ragEngine.processDocument` 或 `ragEngine.reprocessAllDocuments`。
2. `RAGChat` / 员工端回答时在引用列表中明确区分【通用制度】【专属制度】，并显示匹配标签，符合 PRD 对元数据透明的诉求。
3. 设计文档版本替换/重新索引流程：上传新版本时保留 doc id、更新 `version` 字段，界面提示“待重建索引”。

**Step 3 — HITL 邮件 & 链路完善**
1. Edge Function 邮件模板中注入真实链接：链接指向应用中的任务/审批页面，附带 token；应用端提供 `/tasks/:id?token=` 简易验证。
2. 专家任务卡展示完整历史对话（`history` 字段），领导审批驳回时强制填写 `rejectReason` 并自动退回专家，同时 `notifyExpertRejection` 带上原答案。 
3. 审批通过后调用 `notifyEmployee`，使用画像邮箱字段或系统配置的“默认提问者邮箱”列表。

**Step 4 — 共情 Prompt 与“不满意”触发**
1. 深度整合共情规则：在 `deepseek-client` 的 system prompt 写入“事实 / 共情双模式 + 三段式输出”，把 `detectEmotion` 的关键词列表也塞入 Prompt 中。
2. 员工端回答区新增“满意/不满意”按钮并接入上报逻辑：点“不满意”直接弹出闭环选项并将问题记录为 escalation。
3. 保持 `getEmpathyPrefix` 作为兜底，但优先依靠模型遵循的共情格式输出。

**Step 5 — 配置与安全**
1. `system_settings` 扩展字段（联系专家话术、情绪关键词、免责声明模板）；设置界面展示并写回。
2. 邮件和网页链接携带一次性 token + `supabase` 权限校验，确保只有专家/领导能访问对应任务。
3. 更新 PRD 5.2/5.3，说明配置范围和非目标，确保评委理解哪些内容仍由比赛限制。 

## **1\. 修订历史**

| 版本号 | 修订日期 | 修订人 | 修订内容 |
| :---- | :---- | :---- | :---- |
| V1.0 | (原始日期) | (原始作者) | 基础政策解答功能初稿 |
| V2.0 | 2025-11-14 | PRD编写专家 | 整合竞赛亮点（情感、图谱等）初稿 |
| V2.1 | 2025-11-15 | PRD编写专家 | **基于评分标准和可行性，聚焦"学习闭环"和"角色RAG"，优化PRD** |
| V2.2 | 2025-11-18 | AI助手 | **添加实施路线图、项目状态分析与优先级任务清单（含知识库标签管理优化）** |

## **2\. 项目背景与目标**

### **2.1 核心问题 (实用性 40%)**

1. **COE减负**：职业发展COE在政策解答上耗时巨大，咨询量爆发时（每年3月、9月）口径不一，影响员工体验。  
2. **知识断层**：制度更新后，AI无法及时学习；员工的边缘问题(Edge Case)无法被解答和沉淀。  
3. **效能打样**：HR团队希望构建一个“最快落地”的实用产品，为后续的劳动政策、绩效管理等其他COE模块提供可复制的“打样”方案。

### **2.2 需求目标 (V2.1 优化)**

1. **【实用性 40%】精准减负**：自动化处理高频政策问答，将COE从重复工作中解放。  
2. **【创新性 30%】闭环进化**：构建“**员工提问 \-\> AI解答 \-\> 专家审核 \-\> 领导审批 \-\> 自动入库**”的AI+人工协同的**知识学习闭环 (HITL)**。  
3. **【可行性 30%】试点落地**：**由HR团队主导开发**（使用AI辅助或低代码工具），以“**职业发展-人力资源序列**”为试点，快速验证并交付一个高可用的智能体。

## **3\. 用户画像与场景**

### **3.1 目标用户 (试点范围)**

1. **员工用户**：华润啤酒 **人力资源序列** 的员工 (如：COE岗、HRBP岗)。  
2. **管理用户**：  
   * **COE专家 (知识提供者)**：职业发展模块负责人，负责审核和补充知识。  
   * **HR领导 (审批人)**：COE专家的上级，负责对新知识的最终审批。  
   * **IT/HR评委 (决策者)**：关注方案的实用、创新与可行性。

### **3.2 典型用户场景**

* **场景一 (角色化解答)**：人力序列的HRBP小刘（P4-P5级/非管理岗）问：“我们BP的晋升标准是什么？” AI识别其画像后，精准调用“职级评定方案手册”中关于P4-P5非管理岗HRBP的部分进行回答。  
* **场景二 (共情式对话)**：小刘问：“我又没通过晋升，太沮丧了”。AI识别到关键词“沮丧”，自动切换到共情模式回复：“我理解您的失落...根据政策，申诉流程是...”  
* **场景三 (学习闭环)**：小刘问了一个AI知识库没有的政策边缘问题。  
  1. AI：“这个问题我暂时无法回答，您希望我【推荐专家A】还是【上报此问题】？”  
  2. 小刘选择【上报】。  
  3. 系统生成“待办任务”，邮件推送给COE专家。  
  4. 专家**每周一次**登录系统，撰写标准答案，提交“领导审批”。  
  5. HR领导审批【同意】。  
  6. 该问答对**自动更新**到AI知识库中。

## **4\. 功能需求详述**

### **4.1 功能模块一：角色化RAG问答引擎 (P0)**

#### **4.1.1 用户故事 1.1 (员工侧)：用户画像收集**

* **作为** \[首次使用智能体的HR员工\]  
* **当我** \[开始对话时\]  
* **我希望** \[AI能通过几个简单的选择题，快速了解我的背景\]  
* **以便于** \[它后续能给我提供最精准的（符合我身份的）答案\]  
* **业务规则与逻辑**:  
  1. **触发**: 用户首次（或重置会话后）发起对话时触发。  
  2. **询问**: AI应主动、依次询问3个关键问题（均为选择题）：  
     * Q1：“请选择您当前的**专业序列**”（例如：A. 人力资源序列, B. 财务序列...）  
     * Q2：“请选择您当前的**职级范围**”  
       * A. P1-P3  
       * B. P4-P5  
       * C. P6-P7  
     * Q3：“请选择您的**角色类型**”（例如：A. 管理岗, B. 非管理岗）  
  3. **记忆**: AI必须在当前会话中“记住”这些选择，作为后续RAG检索的上下文。  
* **验收标准**:  
  1. 新用户进入时，AI能主动发起这三组提问，且职级选项正确无误。  
  2. 用户完成选择后，AI能正常继续对话。

#### **4.1.2 用户故事 1.2 (AI侧)：角色化RAG检索**

* **作为** \[AI智能体\]  
* **当我** \[收到一个问题时\]  
* **我希望** \[能利用已知的用户画像标签（序列、职级、角色）\]  
* **以便于** \[在向量知识库中进行**带标签的过滤检索**，只匹配最相关的文档\]  
* **业务规则与逻辑**:  
  1. **RAG检索**: AI在执行RAG检索时，**必须**将用户画像（例如：'序列': '人力资源', '角色': '非管理岗'）作为元数据(Metadata)过滤器。  
  2. **匹配逻辑**: AI应优先检索标签完全匹配的知识；其次是通用知识（未打角色标签的，如“通用制度”）。  
  3. **免责声明 (P1)**: 当AI的回答属于“建议类”（如晋升路径、技能发展）时，**必须**在回复末尾自动附加免责声明：“*以上建议仅供参考，员工需结合实际情况衡量可行性，不作为公司承诺。*”  
* **验收标准**:  
  1. 一个“非管理岗”的HR和一个“管理岗”的HR，问同一个晋升问题，能得到基于不同制度的、差异化的回答。  
  2. 建议类回答能自动带上免责声明。

#### **4.1.3 用户故事 1.3 (管理员侧)：知识库标签管理**

* **作为** \[COE专家/系统管理员\]  
* **当我** \[在后台上传一份知识文档（如PDF, Word）时\]  
* **我希望** \[系统能让我为这份文档**打上多个标签**\]  
* **以便于** \[这份知识能被AI在特定场景下精准检索到\]  
* **业务规则与逻辑**:  
  1. **后台界面**: 知识库管理后台在上传文件时，应提供标签（Metadata）填写功能。  
  2. **标签匹配**: 标签的Key（键）必须与用户画像的问题（4.1.1）保持一致，例如：专业序列, 职级范围, 角色类型。  
  3. **维护性**: 知识库应支持“版本更新”或“文件替换”，以便在政策变化时快速维护。  
* **验收标准**:  
  1. 管理员可以成功上传一份PDF，并为其打上'专业序列': '人力资源' 和 '角色类型': '管理岗' 的标签。  
  2. 该文档能被 4.1.2 中的AI检索逻辑正确匹配到。

### **4.2 功能模块二：AI知识学习闭环 (HITL) (P0)**

#### **4.2.1 用户故事 2.1 (员工侧)：触发学习闭环**

* **作为** \[HR序列员工\]  
* **当我** \[AI无法回答我的问题，或者我对答案不满意时\]  
* **我希望** \[系统能提供“自行联系”和“上报问题”两个清晰的选项\]  
* **以便于** \[我的问题总能找到一个解决方案\]  
* **业务规则与逻辑**:  
  1. **触发**: 当AI判断无法回答（或用户点击“不满意”）时，系统应自动展示两个选项：【自行联系专家】、【上报此问题】。  
  2. **【自行联系专家】**: 点击后，AI回复：“如需及时解答，您可以自行联系职业发展COE（润工作搜索xxx，联系电话：xxx）。”  
  3. **【上报此问题】**: 点击后，AI回复：“您的问题已成功上报。COE专家将**每周**集中处理（预计1周内完成解答），答案入库后将更新知识库。感谢您的贡献！”  
* **验收标准**:  
  1. “无法回答”的场景能正确触发两个选项。  
  2. 点击【自行联系专家】能返回正确的联系话术。  
  3. 点击【上报此问题】能返回正确的上报话术，并且该问题被标记为“待处理”并进入专家任务队列。

#### **4.2.2 用户故事 2.2 (COE专家侧)：处理待办任务**

* **作为** \[COE专家\]  
* **当我** \[有员工上报了新问题时\]  
* **我希望** \[能通过邮件收到一个包含详情链接的通知\]  
* **以便于** \[我能在一个界面上集中处理所有待办问题\]  
* **业务规则与逻辑**:  
  1. **通知**: 系统自动向COE专家的邮箱发送一封“新问题待处理”的通知邮件。  
  2. **可配置**: 该“**COE专家邮箱地址**”必须是系统后台**可配置**的（非硬编码），以便未来人员变动。  
  3. **链接**: 邮件中包含一个唯一的URL链接，指向该问题的处理界面。  
  4. **界面**: 点击链接后，专家能看到一个表单，包含：  
     * **问题原文** (只读)  
     * **提问者画像** (例如：P4-P5, 非管理岗, 只读)  
     * **完整对话上下文** (只读)  
     * **标准答案** (富文本填写框)  
     * **提交审批** (按钮)  
* **验收标准**:  
  1. 员工上报问题后，专家能收到邮件。  
  2. 点击邮件链接能打开正确的处理界面，且信息显示完整。  
  3. 专家填写答案后，可以点击“提交审批”。

#### **4.2.3 用户故事 2.3 (HR领导侧)：审批新知识**

* **作为** \[HR领导 (审批人)\]  
* **当我** \[COE专家提交了一个新答案时\]  
* **我希望** \[能通过邮件收到一个包含审批链接的通知\]  
* **以便于** \[我能快速审核并控制知识库的最终质量\]  
* **业务规则与逻辑**:  
  1. **通知**: 专家提交后，系统自动向HR领导的邮箱发送“待审批”通知。该“**HR领导邮箱地址**”也必须是系统后台**可配置**的。  
  2. **界面**: 领导点击链接后，能看到“问题原文”和专家撰写的“标准答案”。  
  3. **决策**: 界面上有两个按钮：【批准】和【驳回】。  
  4. **【批准】**: 该Q\&A对自动加入AI的向量知识库，状态变为“已解决”。  
  5. **【驳回】**: 点击【驳回】时，系统**必须**要求领导填写“驳回理由”。该任务连同理由将返回给COE专家的待办列表（见4.2.2）。  
* **验收标准**:  
  1. 领导能收到审批邮件。  
  2. 【批准】操作能成功将知识写入知识库。  
  3. 【驳回】操作必须填写理由，并能成功将任务退回给专家。

### **4.3 功能模块三：动态共情式回复 (P1 \- Prompt工程)**

#### **4.3.1 用户故事 3.1 (AI侧)：动态语气调整**

* **作为** \[AI智能体\]  
* **当我** \[在System Prompt中被赋予了“职业教练”的角色时\]  
* **我希望** \[我的指令能告诉我如何区分“事实性问题”和“情绪性问题”\]  
* **以便于** \[我能在回答事实时保持专业严谨，在感知到敏感信息时自动切换到共情语气\]  
* **业务规则与逻辑**:  
  1. **定义System Prompt (核心)**: 智能体的“系统指令”是实现本功能的关键。  
  2. **规则A (默认/事实模式)**: “你的主要职责是作为华润啤酒的职业发展政策专家，对**事实性**问题（如流程、时间、标准）的回答必须**简洁、精准、客观**。”  
  3. **规则B (触发/共情模式)**: “**但是**，如果用户的问题**包含**以下任一特征，你必须切换到‘共情教练’模式：”  
     * **(关键词触发)**: 包含“失败”、“沮丧”、“焦虑”、“不公平”、“没通过”、“难过”、“困惑”等词汇。  
     * **(话题触发)**: 明确在询问“申诉流程”、“晋升失败”、“绩效改进”等敏感话题。  
  4. **共情模式执行**: “在‘共情教练’模式下，你的回复**必须**遵循以下结构：  
     * **第一步 (共情)**：先表达理解和安抚。（例如：“我理解你的感受...”）  
     * **第二步 (客观分析)**：再客观地陈述相关的政策或事实。  
     * **第三步 (边界)**：你**不能**提供心理咨询或主动引导，职责是提供政策支持。”  
* **验收标准**:  
  1. **场景一 (事实)**: 提问“职级评定几月开始？”，AI**不会**使用共情语气，而是直接、客观地回答。  
  2. **场景二 (共情)**: 提问“我又没通过晋升，太沮丧了”，AI**必须**先安抚（如：“我理解您的失落...”），然后再提供相关建议或政策。

## **5\. 边界条件与非目标**

### **5.1 试点范围 (可行性 30%)**

* **知识库范围**：仅包含“职业发展”模块，且**优先梳理人力资源序列**的相关文档（如：通用制度、HR序列评定手册等）。  
* **用户范围**：优先开放给**人力资源序列**的“COE岗”和“HRBP岗”进行打样测试。

### **5.2 非功能性需求**

* **审核SLA**：COE专家的审核周期为**每周一次**。AI在引导用户“上报”时应明确告知此预期。  
* **开发主体**：本项目由HR业务人员使用AI辅助或低代码平台进行开发。  
* **配置性**：COE专家和HR领导的“通知邮箱”必须在后台可配置。

### **5.3 非目标 (明确不做)**

* **本次不做1**：复杂的“知识图谱”构建（改为“角色RAG”）。  
* **本次不做2**：“数据可视化驾驶舱”（超出试点范围和非技术团队能力）。  
* **本次不做3**：复杂的多模态交互（如语音、视频）。  
* **本次不做4**：主动的情绪识别算法（改为“Prompt工程触发”）。  
* **本次不做5**：提供任何具有法律效力的政策承诺。

## **6\. Prompt提示词（用于搭建智能体）**

```
{需求分析与细化
基于《职业发展打样AI政策解答与学习闭环智能体 V2.1》，我将智能体定位为：面向职业发展COE和HRBP的角色化政策助手，具备精准问答、学习闭环和共情体验，聚焦“最快落地”的试点需求。以下为落地提示词骨架：

1. 角色化RAG问答引擎（P0）
核心功能：
- 首次/重置会话触发画像问答（专业序列、职级范围、角色类型），画像结果需写入会话上下文并“跨会话持久化”，除非用户主动重置
- 根据画像标签调用向量知识库进行Metadata过滤，优先匹配完全一致的制度文档，其次通用制度
- 建议类回答自动附加中文免责声明：“以上建议仅供参考，员工需结合实际情况衡量可行性，不作为公司承诺。”
- 知识库后台上传/替换文档时可填写多个标签字段，字段允许扩展（如序列、职能、地区）；专家可新增字段并即时触发“重新索引”操作，保证向量库内容同步
- 系统需支持使用不同专业序列的数据进行测试（例如后续专家若给到财务序列文档，也沿用同一标签机制完成测试）
已确认：
- 画像题目为单选且与制度标签一致
- 画像信息在账号层持久保存，复用到下次会话
- 标签体系支持动态扩展，管理员可随时追加字段
- 每次知识库文档更新或标签变动后，后台按钮触发重新索引
- 免责声明仅需中文版本

2. AI知识学习闭环 HITL（P0）
核心功能：
- 当AI无法回答或用户点“不满意”时，弹出【自行联系专家】【上报此问题】
- 【自行联系专家】：返回可配置话术（润工作入口、联系方式）
- 【上报此问题】：提示“COE专家每周集中处理，预计1周内反馈”，并生成待办任务
- 任务创建后发送邮件到可配置的COE专家邮箱（无抄送），邮件包含处理链接；专家在页面查看问题原文、提问者画像、完整聊天记录，并填写标准答案提交审批
- 专家提交后向HR领导发送审批邮件；领导在审批界面可【批准并入库】或【驳回并填写理由】
- 批准：自动写入向量知识库并向提问者发送“知识库已更新”反馈邮件
- 驳回：邮件提醒专家，并附带历史版本/驳回理由，任务回到专家待办
已确认：
- 员工端不展示SLA倒计时，仅后台记录任务状态供专家/领导查看
- 整个流程只有一位职业发展专家负责处理，毋须自动分配
- 邮件无需抄送，但审批通过后要有反馈邮件通知提问者
- 驳回需附带历史答案或上下文，并再次提醒专家处理

3. 动态共情式回复（P1 Prompt工程）
核心功能：
- System Prompt定义“华润啤酒职业发展政策专家 + 职业教练”双重角色
- 内置固定的触发关键词/话题列表（失败、沮丧、焦虑、不公平、没通过、难过、困惑、申诉流程、晋升失败、绩效改进等），无需后台配置；命中即切换共情模式
- 共情模式回复结构：①共情安抚 ②客观政策说明 ③职责边界提醒（不提供心理咨询）；事实模式保持简洁、精准、客观
- 共情回复模板可统一，不要求多样化；无需在日志中标记语气模式

页面结构建议
职业发展智能体
├── 员工对话空间
│   ├── 首次画像问答浮层（可重置）
│   ├── 答案区（展示画像标签、当前语气模式、免责声明）
│   └── 无法回答弹窗（联系专家/上报）
├── 专家待办工作台
│   ├── 待处理列表（问题摘要、画像、提交时间、当前状态）
│   └── 详情页（问题原文、对话上下文、历史版本、答案输入框、提交审批按钮）
├── HR领导审批中心
│   ├── 待审批列表
│   └── 审批页（问题+答案，批准/驳回+必填理由）
├── 知识库管理
│   ├── 文档上传/标签管理/版本替换
│   └── 重新索引按钮与状态提示
├── 任务与洞察（可选）
│   ├── 上报数量、完成率、驳回情况
│   └── SLA提醒（后台可见）
└── 系统设置
    ├── 专家/领导邮箱配置
    ├── 联系专家文案
    └── 情绪关键词/免责声明模板（只读固定亦可）

页面线框（示意）
1. 员工对话
┌────────────────────────────────┐
│ 职业发展助手｜画像：人力资源｜P4-P5｜非管理 │
├────────────────────────────────┤
│ 聊天记录（含“事实/共情”标签）             │
│ …                                          │
├────────────────────────────────┤
│ [联系专家] [上报问题] [重新填写画像]         │
└────────────────────────────────┘

2. 专家待办
┌────────────────────────────────┐
│ 待处理问题（邮件已发送至 xxx@crbeer.com）   │
├────────────────────────────────┤
│ 列表：问题｜画像｜提交时间｜状态             │
│ → 查看详情：问题原文/画像/对话/历史版本      │
│                 标准答案输入框 + [提交审批]   │
└────────────────────────────────┘

3. 领导审批
┌────────────────────────────────┐
│ 待审批：来自专家XXX                             │
├────────────────────────────────┤
│ 问题原文 + 专家答案 + 历史记录                 │
│ [批准并入库] [驳回并填写理由]                  │
└────────────────────────────────┘

数据库结构建议（SQLite）
1. user_profiles：user_id，sequence，level_range，role_type，persisted_at（用于跨会话持久化画像）
2. conversations：question，answer，answer_mode（factual/empathy），disclaimer_used，metadata，profile_id
3. policy_documents：title，file_path，sequence_tag，level_tag，role_tag，extra_tags(JSON)，version，status
4. escalation_tasks：conversation_id，question，reporter_profile(JSON)，status，history(JSON)，created_at，updated_at
5. expert_answers：task_id，answer，submitted_at
6. approvals：task_id，decision，reason，decided_at
7. system_settings：key，value（如专家邮箱、领导邮箱、联系话术、关键词列表、免责声明）

API / 技术实现（以Next.js App Router为例）
/api
├─ conversations/start        # 初始化会话+读取/更新画像
├─ conversations/answer       # 角色化RAG检索+共情判断+免责声明
├─ knowledge/documents        # 文档上传/标签/版本管理
├─ knowledge/reindex          # 触发重新索引（嵌入）
├─ escalations/create         # 上报任务
├─ escalations/expert         # 专家查看/提交答案
├─ escalations/approve        # 领导审批
├─ notifications/email        # 邮件发送（上报、审批、反馈）
└─ settings                   # 邮箱、联系话术、关键词维护
关键技术点：
- 向量库可选PgVector/Milvus/Pinecone，Metadata含 sequence/level/role/extra_tags
- BullMQ/CRON 定期提醒专家处理待办并检查“每周一次”承诺
- 前端状态管理：React Query 或 Zustand；UI 推荐 Tailwind + shadcn/ui
- Prompt工程：在System Prompt中写入事实模式/共情模式规则，情绪关键词来源于代码中的固定列表
- 安全：邮件链接需携带一次性token，区分专家/领导权限

工作流程
员工发起对话 → 画像确认（跨会话读取/如需可重置） → 角色化RAG检索 → 
    · 若命中：输出答案（事实或共情模式，含免责声明）
    · 若未命中：提示【联系专家/上报】 → 用户上报 → 生成任务 → 邮件通知专家
专家处理 → 填写答案 → 提交审批 → 领导审核 →
    · 批准：写入向量知识库 + 发送“已更新”反馈给提问者
    · 驳回：填写理由并附历史版本 → 邮件提醒专家 → 任务回到待办

下一步行动建议
1. 确认画像题目枚举，与制度标签字段一致，并实现账号级持久化读写。
2. 搭建知识库上传 + 标签 + 重新索引流，确保可扩展字段随时生效。
3. 先实现"最小可行"对话（画像/RAG/免责声明）与单专家上报-审批链路，再补邮件模板、历史版本、反馈通知。
4. 将固定的情绪关键词写入System Prompt或配置文件，验证共情模式触发逻辑。
5. 安排不同序列文档的测试计划，确保未来更换序列时无需改动流程，仅更新标签与知识库即可。
}
```

---

## **7. 实施路线图与下一步行动** 🚀

### **7.1 项目当前状态**

根据2025年11月18日的项目分析，当前项目完成度约为 **75-80%**。核心RAG问答引擎、对话引擎、知识库管理和大部分HITL工作流已基本完成。前端界面也已搭建完毕。

**已完成模块：**
- ✅ 用户画像收集与持久化
- ✅ 基础RAG检索引擎
- ✅ 向量知识库（基于本地JSON存储）
- ✅ 知识库文档上传与标签设置
- ✅ HITL工作流基础架构（上报、专家处理、领导审批）
- ✅ 前端界面（员工对话、管理后台、知识库管理）
- ✅ 共情模式Prompt工程

**待完善模块：**
- ⚠️ 邮件通知系统（未实现）
- ⚠️ 知识库标签编辑功能（后端API已有，前端界面缺失）
- ⚠️ RAG检索的"通用"与"专属"逻辑优化
- ⚠️ 系统设置持久化
- ⚠️ 端到端测试与性能优化

### **7.2 优先级任务清单**

#### **优先级1：完成邮件通知系统 (P0 - 重要)**

**目标**：实现HITL工作流中的邮件通知功能，确保专家和领导能及时收到任务通知，员工能收到知识更新反馈。

**任务清单**：
1. **配置 `.env` 文件**：
   - 设置 SMTP 服务器信息（例如：`EMAIL_HOST`, `EMAIL_PORT`, `EMAIL_USER`, `EMAIL_PASS`）
   - 如果使用特定邮件服务（如 Gmail、SendGrid），请查阅其SMTP配置文档。

2. **完善 `lib/email/sender.ts`**：
   - 导入并配置 `nodemailer` 库。
   - 实现发送邮件的通用函数，包括邮件内容（主题、正文HTML/文本）、收件人等参数。
   - 创建邮件模板（例如：`lib/email/templates.ts`），用于专家通知、领导审批通知、知识更新反馈通知。

3. **集成邮件通知到 HITL API**：
   - 在 `app/api/hitl/report/route.ts` 中，当员工上报问题时，调用邮件发送函数通知COE专家。
   - 在 `app/api/hitl/expert/route.ts` 中，当专家提交答案时，调用邮件发送函数通知HR领导。
   - 在 `app/api/hitl/leader/route.ts` 中，当领导批准知识入库时，调用邮件发送函数通知提问员工。
   - 当领导驳回时，通知COE专家，并附带驳回理由。

**预计时间**：1-2小时

**验收标准**：
- 员工上报问题后，专家能收到邮件通知
- 专家提交答案后，领导能收到审批邮件
- 领导批准后，员工能收到知识更新反馈邮件
- 领导驳回后，专家能收到驳回通知邮件

---

#### **优先级2：知识库标签管理与RAG优化 (P0 - 重要)**

**目标**：实现知识库文档标签与用户画像的深度勾稽，优化RAG检索逻辑，确保AI能根据用户画像提供更精准、分层的知识解答，并支持管理员对已有文档标签进行修改。

**任务清单**：

1. **优化向量检索的"通用"逻辑**：
   - 修改 `lib/vector-store.ts` 中的 `searchDocuments` 函数的过滤逻辑，明确将 `tag_sequence` 为 "全员通用"、`tag_level` 为 "全职级"、`tag_role` 为 "全角色" 的文档视为通用内容。
   - 实现职级范围匹配功能，例如 P2 用户可以检索到标签为 "P1-P3" 或 "全职级" 的文档。
   - 确保标签为空的文档不再被默认视为通用内容。

2. **优化聊天检索与上下文构建**：
   - 调整 `app/api/chat/route.ts` 中的 RAG 检索逻辑，确保第一层明确搜索"通用"标签的文档。
   - 调整第二层搜索用户画像匹配内容时，排除已在通用内容中明确包含的文档，避免重复。
   - 优化 `getSystemPrompt` 中构建上下文的格式，更清晰地标注文档来源和匹配标签信息（例如："【通用制度 | 《文档名》 | 标签：全员通用】" 和 "【专属制度 | 《文档名》 | 匹配标签：序列:人力资源 | 职级:P1-P3】"）。

3. **实现前端标签编辑功能**：
   - 在 `app/admin/knowledge/page.tsx` 的文档列表中为每个文档添加"编辑"按钮。
   - 点击"编辑"后，文档的标签显示为可编辑的下拉框或输入框。
   - 添加"保存"和"取消"按钮，调用 `pages/api/admin/knowledge.ts` 中的 `PATCH` 方法更新文档标签。
   - 确保标签更新后，文档会自动触发重新索引，更新向量库中的元数据。

**预计时间**：2-3小时

**验收标准**：
- 不同用户画像（如人力资源序列 vs 技术序列）提问相同问题时，能获得差异化的回答
- AI回答时能明确标注"通用制度"和"专属制度"的来源
- 管理员可以在后台修改已上传文档的标签，修改后自动重新索引
- P2用户能检索到标记为"P1-P3"的文档内容

---

#### **优先级3：系统设置持久化 (P1 - 次要)**

**目标**：实现管理后台系统设置的持久化存储，允许管理员配置COE专家邮箱、HR领导邮箱等关键参数。

**任务清单**：
1. **完善 `app/api/admin/settings/route.ts`**：
   - 实现 `POST` 请求处理逻辑，将前端提交的配置数据保存到 `settings` 数据库表。
   - 实现 `GET` 请求处理逻辑，从 `settings` 表中读取配置数据并返回给前端。

2. **更新前端设置页面**：
   - 在 `app/admin/settings/page.tsx` 中，确保表单数据能够正确地从后端读取和保存。
   - 移除或替换"当前为演示模式，实际保存功能待开发"的提示信息。

**预计时间**：30分钟

**验收标准**：
- 管理员可以在设置页面修改COE专家邮箱、HR领导邮箱等配置
- 配置保存后，刷新页面能正确显示已保存的配置
- 邮件通知功能能读取并使用最新的邮箱配置

---

#### **优先级4：端到端测试与优化 (P1)**

**目标**：对整个系统进行全面测试，发现并修复潜在问题，提升用户体验和系统性能。

**任务清单**：
1. **准备测试文档**：
   - 上传至少一份通用制度文档（标签：全员通用/全职级/全角色）。
   - 上传针对不同用户画像的专属制度文档（例如：人力资源序列 P1-P3 基层、人力资源序列 P4-P5 经理级）。

2. **进行对话测试**：
   - **角色化RAG测试**：使用不同用户画像提问相同问题，验证AI是否能根据画像提供差异化回答。
   - **共情模式测试**：使用包含情绪词汇的问题（如"沮丧"、"焦虑"），验证AI是否能切换到共情模式并给出安抚性回复。
   - **免责声明测试**：验证在建议类回答中是否自动附加免责声明。

3. **测试HITL工作流**：
   - **上报流程**：模拟员工上报问题，验证任务是否成功创建并通知专家。
   - **专家处理**：模拟专家处理问题、填写答案并提交审批，验证流程是否顺畅。
   - **领导审批**：模拟领导批准和驳回操作，验证知识库更新和驳回逻辑。
   - **邮件通知**：验证所有邮件（通知、反馈）是否正常发送和接收。

4. **Prompt调优**：
   - 根据测试结果，对 `lib/llm.ts` 或 `app/api/chat/route.ts` 中的 System Prompt 进行迭代优化，特别是共情模式的触发和回复效果。

5. **性能优化**：
   - 关注控制台日志中的RAG检索时间和LLM调用时间，进行必要的优化（例如：调整RAG检索数量、优化数据库查询）。

**预计时间**：2-3小时

**验收标准**：
- 所有核心功能通过测试，无阻塞性bug
- 共情模式能正确触发并提供恰当的回复
- HITL工作流完整运行，邮件通知正常
- 系统响应时间在可接受范围内（RAG检索 < 2秒，LLM响应 < 5秒）

---

#### **优先级5：部署准备 (P1)**

**目标**：为项目上线生产环境做好准备，确保系统稳定可靠。

**任务清单**：
1. **编写部署文档**：
   - 详细说明生产环境的部署步骤，包括服务器环境、依赖安装、配置部署等。
   - 提供常见问题（FAQ）和维护手册。

2. **生产环境变量配置**：
   - 确保 `.env` 文件中的敏感信息使用生产环境的密钥和配置。
   - 考虑使用环境变量管理工具（如 `dotenv`）或云平台自带的环境变量管理。

3. **数据库和向量库备份策略**：
   - 为 SQLite 数据库文件 (`database/career-ai.db`) 和向量库文件 (`data/vectors.json`) 设计定期备份方案，防止数据丢失。

4. **准备演示数据**：
   - 如果需要，准备一套完整的演示数据，以便于项目演示和快速部署。

**预计时间**：1-2小时

**验收标准**：
- 部署文档完整，第三方可以根据文档独立完成部署
- 生产环境配置正确，系统能稳定运行
- 备份策略已实施，数据安全有保障
- 演示数据准备完毕，可用于产品演示

---

### **7.3 实施时间线建议**

**第1周（当前）**：
- Day 1-2：完成邮件通知系统（优先级1）
- Day 3-4：完成知识库标签管理与RAG优化（优先级2）
- Day 5：完成系统设置持久化（优先级3）

**第2周**：
- Day 1-3：端到端测试与优化（优先级4）
- Day 4-5：部署准备与文档编写（优先级5）

**预计总时间**：7-10个工作日

---

### **7.4 成功标准**

完成以上所有任务后，职业发展AI智能体将达到以下状态：

✅ **功能完整性**：所有P0功能（角色化RAG、HITL工作流、邮件通知）全部实现并通过测试

✅ **用户体验**：员工能获得精准、个性化的政策解答；专家和领导能高效处理上报问题

✅ **系统稳定性**：经过充分测试，无重大bug，性能满足要求

✅ **可维护性**：代码结构清晰，配置灵活，文档完善

✅ **可扩展性**：标签体系支持扩展，可快速适配其他专业序列和业务场景

**此时，系统将达到一个非常完善且可投入试点使用的状态，为华润啤酒职业发展COE提供实质性的减负效果。** 🎉
